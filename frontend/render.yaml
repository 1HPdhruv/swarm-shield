services:
  - type: web
    name: swarm-shield-dashboard
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://swarm-shield-api.onrender.com